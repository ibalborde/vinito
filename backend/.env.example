# Base de datos
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE"

# Servidor
PORT=3000
NODE_ENV=development

# JWT
# Generar en producción con: openssl rand -base64 32
JWT_SECRET=cambiar_por_un_string_largo_y_aleatorio
JWT_EXPIRES_IN=7d
